# Dependencies
node_modules/

# Build output
dist/
dist-ssr/

# Environment — NEVER commit these (set values in CF Pages + Railway dashboards instead)
.env
.env.local
.env.development
.env.development.local
.env.test.local
.env.production
.env.production.local

# ML model artifacts — deploy separately, not via git
ml/model/*.pkl
ml/model/training_data.csv
ml/model/__pycache__/
ml/__pycache__/
ml/.venv/
ml/venv/
*.pyc
__pycache__/

# Claude / Cowork internals — never commit these
.claude/

# Playwright auth states — contain raw JWTs, must NOT be committed
playwright/.auth/

# Playwright test artifacts
playwright-report/
test-results/

# Editor
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# TypeScript
*.tsbuildinfo

# Vite
*.local
