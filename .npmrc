# PMO Platform — npm config
# Force npm to always install devDependencies regardless of NODE_ENV.
# Without this, `npm install` silently skips vite, typescript, and all
# build tools when NODE_ENV=production is set in the shell environment.
include=dev
