# PMO Platform — npm config
# IMPORTANT: npm omits devDependencies when NODE_ENV=production (which is set in
# this shell environment). Always install using `npm run setup` which overrides it.
# See package.json scripts.setup for the full install command.
