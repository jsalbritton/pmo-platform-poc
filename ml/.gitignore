# Environment variables — NEVER commit these
.env

# Trained model artifacts — large binary files, regenerated by running train_risk_model.py
model/risk_model.pkl
model/feature_importance.png

# Python cache
__pycache__/
*.pyc
*.pyo
